# Core requirements
-r requirements-core.txt

# Development dependencies
-r requirements-dev.txt

# Uncomment optional components as needed:
-r requirements-ocr.txt      # OCR capabilities (requires Tesseract)
-r requirements-table.txt    # Table extraction (requires Ghostscript)
-r requirements-audio.txt    # Audio transcription (requires ffmpeg & LLVM)