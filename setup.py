"""Setup script for ecological-extractor package."""

from setuptools import setup  # type: ignore

# This setup.py file is maintained for compatibility with older tooling.
# Most configuration is in pyproject.toml

if __name__ == "__main__":
    setup()
